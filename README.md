# Frontend

## Quick Start

1. Install dependencies:

```bash
npm install
```

2. Set up environment variables:

```bash
cp .env.sample .env
# then update .env with proper values
```

3. Run the project:

```bash
npm run dev
```

The app should now be running locally, usually at [http://localhost:5173](http://localhost:5173).

```

```
